// @generated
// This file is automatically generated by Kanel. Do not modify manually.

import { z } from "zod";
import { NumRange } from "../../../types/api/Ranges.js";
import { roleId, type RoleId } from "./Role.js";
import { salaryPeriod, type default as SalaryPeriod } from "./SalaryPeriod.js";

/** Identifier type for hire_me.salary */
export type SalaryId = number & { __brand: "SalaryId" };

/** Represents the table hire_me.salary */
export default interface Salary {
	id: SalaryId;

	role_id: RoleId;

	salary_range: NumRange;

	salary_includes_super: boolean;

	salary_period: SalaryPeriod;

	salary_currency: string;
}

/** Represents the initializer for the table hire_me.salary */
export interface SalaryInitializer {
	role_id: RoleId;

	salary_range: NumRange;

	salary_includes_super: boolean;

	salary_period: SalaryPeriod;

	salary_currency: string;
}

/** Represents the mutator for the table hire_me.salary */
export interface SalaryMutator {
	role_id?: RoleId;

	salary_range?: NumRange;

	salary_includes_super?: boolean;

	salary_period?: SalaryPeriod;

	salary_currency?: string;
}

export const salaryId = z.number() as unknown as z.Schema<SalaryId>;

export const salary = z.object({
	id: salaryId,
	role_id: roleId,
	salary_range: z.object({
		min: z.number().nullable(),
		max: z.number().nullable(),
	}),
	salary_includes_super: z.boolean(),
	salary_period: salaryPeriod,
	salary_currency: z.string().min(1),
}) as unknown as z.Schema<Salary>;

export const salaryInitializer = z.object({
	id: salaryId.optional(),
	role_id: roleId,
	salary_range: z.object({
		min: z.number().nullable(),
		max: z.number().nullable(),
	}),
	salary_includes_super: z.boolean(),
	salary_period: salaryPeriod,
	salary_currency: z.string().min(1),
}) as unknown as z.Schema<SalaryInitializer>;

export const salaryMutator = z.object({
	id: salaryId.optional(),
	role_id: roleId.optional(),
	salary_range: z
		.object({
			min: z.number().nullable(),
			max: z.number().nullable(),
		})
		.optional(),
	salary_includes_super: z.boolean().optional(),
	salary_period: salaryPeriod.optional(),
	salary_currency: z.string().min(1).optional(),
}) as unknown as z.Schema<SalaryMutator>;
